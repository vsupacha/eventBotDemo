const fs = require('fs');

const VisitorLogModel = require('../models/visitor');
const MapLogModel = require('../models/map_log');


// generate location over map
exports.handleMap = (req, res) => {
    var resp = {}

    // Q3.1 extract userId and lat/lng from req.body 
        
}
    
    

        